// Kept for reference — the chat-driven SosSetupPage manages its own step state inline.
// These types are no longer used but preserved for potential future reference.

export type SosChatStep = 'traps' | 'consequences' | 'response' | 'done'
